// URL 경로에 아무것도 치지 않아도 나오는 기본 page

import Typography from "@mui/material/Typography";

export default function LandingPage() {
  return <Typography>Welcome to a page in the dashboard!</Typography>;
}
